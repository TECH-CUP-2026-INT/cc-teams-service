spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/teams_test_db

app:
  jwt:
    secret: test-secret-key-that-is-at-least-256-bits-long-for-hs256-algorithm

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.0
