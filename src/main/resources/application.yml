<<<<<<< HEAD
# Base configuration for cc-teams-service.
# Manages team creation, membership, captaincy and invitations.
# JWT secret must match the one used by cc-identity-service.

spring:
  application:
    name: cc-teams-service
  profiles:
    active: dev

server:
  # CONFIRM PORT WITH THE OTHER MICROSERVICES TEAMS
  port: 11712

app:
  jwt:
    secret: ${JWT_SECRET:super-secret-key-that-is-at-least-256-bits-long-for-hs256}
=======
spring:
  application:
    name: service-teams

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/teams_service}
      uuid-representation: standard

server:
  port: ${SERVER_PORT:8082}

# Identity Service (JWT validation delegated remotely — no shared secret here)
identity:
  service:
    base-url: ${IDENTITY_SERVICE_URL:http://localhost:8081}

# Communications Service
communications:
  base-url: ${COMMUNICATIONS_URL:http://localhost:8083}

# Tournament Service (mk-tournament-service) — sin server.port propio en ese
# repo, corre en el puerto 8080 por defecto de Spring Boot
tournament:
  service:
    base-url: ${TOURNAMENT_SERVICE_URL:http://localhost:8080}

# Logging
logging:
  level:
    root: INFO
    co.edu.escuelaing.techcup.teams: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
>>>>>>> a28c0a12e67422445e723ae92ff92c31dad62065
